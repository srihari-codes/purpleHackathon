# Store Intelligence API — Python dependencies
# This file covers only the API layer (app/).
# Detection pipeline deps (ultralytics, opencv, etc.) are in the root requirements.txt.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.5.0
httpx>=0.27.0          # for TestClient in tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
