# ──────────────────────────────────────────────────
# Store Intelligence API — Python Dependencies
# ──────────────────────────────────────────────────

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Database
sqlalchemy==2.0.30

# HTTP client (for pipeline → API calls)
requests==2.31.0

# Dashboard
rich==13.7.1

# Computer Vision (pipeline — heavy, not needed for API-only container)
# Uncomment when running the detection pipeline:
# ultralytics==8.2.18       # YOLOv8 + ByteTrack
# opencv-python-headless==4.9.0.80
# torch==2.3.0
# torchvision==0.18.0
# torchreid @ git+https://github.com/KaiyangZhou/deep-person-reid.git

# Testing
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0
