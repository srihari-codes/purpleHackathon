# Detection pipeline requirements
# Pin major versions for reproducibility; allow patch updates.

# ── Core CV / Detection ───────────────────────────────────────────────────
ultralytics>=8.2.0,<9.0        # YOLOv8 + ByteTrack (built-in)
opencv-python-headless>=4.9.0  # headless — no GUI display deps needed

# ── Re-ID (OSNet) ─────────────────────────────────────────────────────────
# torchreid is optional; pipeline falls back to HOG+color histogram if absent.
# Uncomment to enable full OSNet re-ID:
# torchreid>=1.4.0

# ── PyTorch (CUDA or CPU) ────────────────────────────────────────────────
# Install the CUDA variant when running on GPU host:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# The line below installs CPU-only torch as a safe default inside Docker.
# The CUDA variant is installed by docker-compose via the pip override below.
torch>=2.1.0
torchvision>=0.16.0

# ── Web GUI / API ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── OCR for timestamp watermark ───────────────────────────────────────────
pytesseract>=0.3.10

# ── Utilities ─────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
scipy>=1.12.0               # used by ByteTrack internals
Pillow>=10.2.0
python-multipart>=0.0.9     # for FastAPI form data
