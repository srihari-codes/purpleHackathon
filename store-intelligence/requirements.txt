# Detection pipeline requirements
# Pin major versions for reproducibility; allow patch updates.
#
# NOTE: torch and torchvision are intentionally NOT listed here.
# They are installed by the Dockerfile with the correct CUDA/CPU wheel
# so that torch.cuda.is_available() returns True on GPU hosts.
# If running outside Docker, install manually:
#   GPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU:  pip install torch torchvision

# ── Core CV / Detection ───────────────────────────────────────────────────
# YOLO11 requires ultralytics>=8.3.0 (spec: YOLO11m or YOLO11s)
ultralytics>=8.3.0,<9.0        # YOLO11 + ByteTrack (built-in)
opencv-python-headless>=4.9.0  # headless — no GUI display deps needed

# ── Re-ID (OSNet) ─────────────────────────────────────────────────────────
# torchreid is opt-in. Set USE_OSNET=1 env var to enable.
# Adds ~1.5 GB to image; falls back to HSV+colour histogram when disabled.
# torchreid>=1.4.0

# ── Web GUI / API ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── OCR for timestamp watermark ───────────────────────────────────────────
pytesseract>=0.3.10

# ── Utilities ─────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
scipy>=1.12.0               # used by ByteTrack internals
lap>=0.5.12                 # ByteTrack dependency (pre-install to avoid runtime auto-update)
Pillow>=10.2.0
python-multipart>=0.0.9     # for FastAPI form data
